TASK = {
    "task_id": "hard-003",
    "pr_title": "Add task import endpoint with serialisation support",
    "pr_description": "Allows importing task objects from uploaded files. Uses pickle for deserialisation.",
    "max_steps_override": 12,
    "diffs": [
        {
            "filename": "routes/import_tasks.py",
            "language": "python",
            "lines": [
                "import pickle",
                "from flask import Blueprint, request, jsonify",
                "",
                "import_bp = Blueprint('import', __name__)",
                "",
                "@import_bp.route('/tasks/import', methods=['POST'])",
                "def import_tasks():",
                "    raw = request.get_data()",
                "    tasks = pickle.loads(raw)",
                "    db.bulk_insert_tasks(tasks)",
                "    return jsonify({'imported': len(tasks)}), 200",
            ],
            "changed_line_numbers": [9],
        },
        {
            "filename": "routes/forms.py",
            "language": "python",
            "lines": [
                "from flask import Blueprint, request",
                "",
                "forms_bp = Blueprint('forms', __name__)",
                "",
                "@forms_bp.route('/submit', methods=['POST'])",
                "def submit_form():",
                "    data = request.form",
                "    process_form(data)",
                "    return 'OK'",
            ],
            "changed_line_numbers": [6],
        },
        {
            "filename": "shared/counter.py",
            "language": "python",
            "lines": [
                "import threading",
                "",
                "request_count = 0",
                "lock = threading.Lock()",
                "",
                "def increment():",
                "    global request_count",
                "    request_count += 1",
                "",
                "def get_count():",
                "    return request_count",
            ],
            "changed_line_numbers": [7, 8],
        },
    ],
    "ground_truth_bugs": [
        {
            "line": 9,
            "severity": "critical",
            "description": "Deserialization of user-controlled input with pickle is a critical remote code execution vulnerability. An attacker can craft a pickle payload to execute arbitrary code on the server. Use a safe format like JSON instead.",
            "file": "routes/import_tasks.py",
        },
        {
            "line": 8,
            "severity": "critical",
            "description": "Race condition in shared/counter.py: increment() reads and writes request_count without holding the lock. Under concurrent requests, updates will be lost. Use `with lock:` around the read-modify-write operation.",
            "file": "shared/counter.py",
        },
        {
            "line": 6,
            "severity": "warning",
            "description": "Missing CSRF token check on /submit POST endpoint in routes/forms.py. State-changing POST endpoints must validate CSRF tokens to prevent cross-site request forgery attacks.",
            "file": "routes/forms.py",
        },
    ],
}
